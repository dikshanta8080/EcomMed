server:
  port: 8081
  servlet:
    context-path: /api/v1
spring:
  application:
    name: EcomMed
  profiles:
    active: dev
  security:
    user:
      name: root
      password: root
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 15MB
